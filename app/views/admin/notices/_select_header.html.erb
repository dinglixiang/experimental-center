<h4>
  <i class="icon-list-alt"></i>
  <span class="text-info header-state"> <%= link_to t('system.notice'),admin_notices_path %> > <%= yield :state%></span>
  <%= link_to "添加通知", new_admin_notice_path, class: "pull-right"%>
 </h4>

<%= form_tag(search_admin_notices_path, method: :get, class: "navbar-form pull-left") do %>
  <%= select_tag "state", options_for_select(["",["未审核",1],["审核通过",2],["未通过",3]])%>
  <%= submit_tag "过滤", class: "btn btn-primary"%>
<% end %>
